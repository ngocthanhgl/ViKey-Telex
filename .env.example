# GitHub Token — used by `gh` CLI for pushing, CI, releases
# Already stored at ~/.config/gh/hosts.yml — copy here if needed for scripts
# GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# Release signing (CI secrets; not needed for local dev)
# KEYSTORE_BASE64=
# KEYSTORE_PASSWORD=
# KEY_ALIAS=
# KEY_PASSWORD=
